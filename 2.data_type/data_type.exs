# Numeric
age = 10
moneyAmount = 10.5

# Atoms
:hello

# String
name = "Bob"

# Bool
isActive = true

IO.puts("age: #{inspect(age)}")
IO.puts("moneyAmount: #{inspect(moneyAmount)}")
IO.puts(":hello: #{inspect(:hello)}")
IO.puts("name: #{inspect(name)}")
IO.puts("bool: #{inspect(isActive)}")
